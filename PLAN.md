# PLAN.md — build order (check off as you go)
- [x] 1A Fixture skeleton — FastAPI + Pydantic Note models + /api/jobs (in-memory) + static reader + real demo video id + fixture notes + 2-3 stable placeholder frames
- [x] 1B Deploy immediately — minimal Dockerfile; verify on public URL: job poll, YouTube player, seekTo, frame preview
    - DONE: deployed at https://luma-video-notes.onrender.com; trust moment verified in browser; IFrame origin + favicon console noise fixed.
- [x] 2  Grounding rule — enforce grounded rendering only (no frameRef/transcript span -> not shown as grounded); other fields stay loose
    - DONE: claim-level grounding (app/grounding.py helper + defensive frontend); ungrounded claims shown-but-marked, not clickable; one ungrounded fixture claim; grounding tests added.
- [x] 3  One real block — split into 3A (ingest path) then 3B (one vision call):
    - [x] 3A Ingest probe (diagnostic) — DONE. Standalone diagnostics, fixed to the demo video, NOT wired into jobs: app/ingest.py + GET /api/_probe/ingest and app/transcript.py + GET /api/_probe/transcript. Added yt-dlp + youtube-transcript-api (pip) and ffmpeg + nodejs (system pkgs).
        - Findings (host-probe ground truth): LOCAL (residential IP) both paths work — video downloads + 3 frames extract; transcript = 167 segments. RENDER (datacenter IP) both BLOCKED by YouTube — ingest ok=false (HTTP 429 + `Sign in to confirm you're not a bot`; diagnostics confirm ffmpeg+node present, so it's the bot wall, NOT missing deps; the `player_client=android,ios,tv` long-shot did NOT clear it); transcript ok=false (`RequestBlocked` — cloud-provider IPs broadly blocked).
        - Locked strategy: the deployed app will NOT fetch from YouTube at request time (no cookies/login/proxies — not reviewer-reproducible). 3B pre-extracts real frames + transcript from a residential IP, commits them as demo artifacts, and the server reads them reproducibly. Probes/diagnostics stay as evidence; fixture flow + POST /api/jobs untouched.
    - [x] 3B One real visual block from committed demo artifacts — DONE. Browser-verified: the real code block renders with a high-confidence badge, clicking it seeks the player to 6:11, and the source panel shows the real frame-0027.
        - Prep: `scripts/create_demo_artifacts.py` (residential IP) committed `static/demo/GDm_uH6VxPY/frames/` (36 frames @ 640x360, ~0.75 MiB) + `data/demo/GDm_uH6VxPY/{transcript.json (167 segments), manifest.json, contact_sheet.jpg}`. Downloaded video stays in gitignored `work/`; pack sized to serve BOTH 3B and Step 4.
        - Vision: `app/vision.py` makes ONE Claude vision call (Messages API + base64 image; strict-JSON {type,content,confidence}; model from `ANTHROPIC_MODEL`, default `claude-sonnet-4-6`). `scripts/build_one_block.py` ran it once on frame-0027 (6:11) -> `data/demo/GDm_uH6VxPY/{vision_frame_0027.json, note_3b.json}`. Result: type=code, confidence=high — agent code (ParallelAgent + SequentialAgent) read faithfully. Cost guard: cached, no re-call unless `--force`.
        - Wiring: `app/jobs.py` serves `note_3b.json` for the demo URL (fixture otherwise); endpoints unchanged, NO request-time vision. Dockerfile ships `data/`. Single frames registry (all `/static/demo/...`); grounding intact (1 verifiable block, 0 unverifiable). Step 2 ungrounded-claim demo retained on the fixture path.
        - Other Step-4 vision candidates off the contact sheet: diagram frame-0025 (5:43) / frame-0003 (0:35) / frame-0016 (3:37); table/comparison frame-0030 (6:53) / frame-0032 (7:21); code frame-0008 (1:46).
- [x] 4  Widen pipeline — add dedup, OCR/text gate, multi-section, structure pass onto the 3-slice (widen, not rebuild). Split 4A (frame selection) / 4B (vision over candidates) / 4C (structure pass → multi-section note):
    - [x] 4A Deterministic frame selection (dedup + OCR/text gate) — DONE. Build-time analysis over the 36 committed frames, committed as an artifact (NO vision, NO LLM router, NO structure pass; the deployed app reads the JSON and does NOT run OCR at request time).
        - `app/frame_select.py`: phash dedup vs the running representative + a Tesseract text gate, with the decision logic (`select()`/`phash_distance()`) split from the image/OCR I/O so it's unit-testable without the binary. Tuned knobs, ground-truthed on the contact sheet: `DEDUP_HAMMING_THRESHOLD=6` (held-slide ≤6 vs distinct-slide ≥8 — never merges two different slides), `CONF_FLOOR=40`, `MIN_TEXT_SCORE=8` (confident words).
        - `scripts/analyze_demo_frames.py` → `data/demo/GDm_uH6VxPY/frame_analysis.json` (per-frame phash / dedup verdict / ocrText / textScore / isVisionCandidate / candidateReason / suggestedVerifyTimestampSec = max(ts−3,0)).
        - Result: 36 frames → 34 representatives (collapsed frame-0025/0026 into the held diagram frame-0024) → **11 vision candidates** spanning code (0008/0009/0020/0027/0028), table (0030), diagram (0013/0024), text (0004/0032/0033). Sparse title slides + label-light diagrams (0002/0003/0006/0016/0005/0014/0036) fall below the gate — the documented APPROACH.md text-gate limitation, source frame always shown.
        - Deps: imagehash + pytesseract (requirements, build-time only); `tesseract-ocr` added to the Dockerfile apt layer (CMD/$PORT untouched). Tests: `tests/test_frame_select.py` pins the pure dedup/gate rules + the committed-artifact contract (binary-free); `pytest -q` green (13).
    - [x] 4B Vision over the 4A candidates — DONE. Build-time batch over ONLY the 11 deterministic candidates (never the non-candidates), one block per frame, committed as an artifact (deployed app reads it; NO request-time vision). Structure pass still pending (4C).
        - `scripts/build_visual_blocks.py` → `data/demo/GDm_uH6VxPY/visual_blocks.json`. Reuses the Step 3B single client (`app.vision.extract_block`); model `claude-sonnet-4-6` (ANTHROPIC_MODEL override). Each call gets that frame's 4A `ocrText` + `candidateReason` + nearby transcript as CONTEXT-ONLY hints — `app/vision.py` extended (`ocr_hint`/`candidate_reason` params + token-usage capture) to frame them as noisy and tell the model the IMAGE is ground truth (correct/complete the OCR, never copy verbatim).
        - Strict success rule (`classify_result`): empty content / invalid type / invalid confidence / unparsable JSON / API error → a FAILED entry with `error`, never a block. Cost guard: reuses cached successful blocks (resume); `--force` re-calls all. Verified a plain re-run makes 0 API calls.
        - Result: **11/11 blocks, 0 failures** — 5 text, 3 code, 2 diagram, 1 table; ~$0.064, ~5s/call. Ground-truthed against the frames: frame-0027 code (model fixed OCR garbage `ParalletAgent`→`ParallelAgent`), frame-0030 Pros/Cons table, frame-0024 parallel-agent diagram all faithful.
        - Tests: `tests/test_visual_blocks.py` (no Anthropic) pins the pure success/failure rule + the committed-artifact contract (one entry per candidate, ≤1 block/frame, refs resolve, verify-lead ≤ timestamp). `pytest -q` green (16).
    - [x] 4C Structure pass → multi-section hierarchical note — DONE. ONE build-time Claude call turns transcript + the 11 committed VisualBlocks into a section PLAN only (logical topics + which blocks/anchors go where); deterministic assembly builds the Note from the UNCHANGED blocks. Committed + served; deployed app reads it, no request-time model call.
        - `app/structure.py`: `request_section_plan` (one Messages call; same client/model as vision; the model NEVER sees full block content — only frameRef/type/time/~110-char gist — and NEVER outputs block content) + pure `assemble_note` (copies blocks verbatim, one frames registry from the manifest, each block in ≤1 section, anchor timestamps derived from frame_analysis `suggestedVerifyTimestampSec` not the model, unknown blockRef dropped w/ issue, unresolved anchor demoted to ungrounded keeping the claim).
        - `scripts/build_full_note.py` → `section_plan.json` + `structure_raw.json` (model id, no keys) + `note_full.json` (Pydantic-validated). Cost guard: reuses cached `section_plan.json` (0 calls) unless `--force`; verified a re-run rebuilds note_full byte-identically.
        - Result: **5 topic sections** (Intro → Single → Sequential → Parallel → Recap), all **11/11 blocks placed**, **8 grounded / 0 ungrounded** anchors. Every anchor uses the verify-lead (frame-0027 claim seeks to 6:08, not 6:11). `app/jobs.py` now serves `note_full.json` for the demo URL (fixture for other URLs); endpoint shapes unchanged.
        - Tests: `tests/test_full_note.py` (no Anthropic) — committed-note contract + pure assembly rules (unknown/dup/unresolved refs) + demo endpoint serves the multi-section note. `pytest -q` green (21).
        - (4C-era cosmetic limit — non-code blocks rendered as literal `##`/`**`/`|` — RESOLVED in 5B: the reader now renders real headings/bold/tables and shows diagram blocks as their frame image.)
- [x] 5  Note readability v2 — bullets-with-anchors (gist + bullets replace detail + claims; transcript-span grounding; visual blocks nested under their bullet). Split 5A (data) / 5B (rendering):
    - [x] 5A Data restructure — DONE (data only; reader is 5B). Sections → gist + bullets[]; Bullet {text, anchor?, blocks[]}; detail dropped; Anchor reshaped to a source-pointer (kind frame|transcript, transcriptText, no claimRef). Grounding gained the transcript-span tier (bullet_tier → visual|spoken|ungrounded). Structure pass emits bullets directly + owns block↔bullet matching + semantic filter; assemble_note stays deterministic (blocks verbatim, one block ≤1 bullet, visual anchor = verify-lead, spoken anchor snapped to real segment.start). Result: 5 sections, 18 bullets (9 visual / 9 spoken), frame-0028 semantic-filtered as junk. Reused 4A/4B (no vision re-run). pytest 20.
        - Dedup follow-up — DONE: deterministic same-bullet text-overlap dedup in assemble_note (DEDUP_TEXT_OVERLAP=0.75) — near-superset same-type blocks collapse to the fuller one, kept verbatim, in assembly only (section_plan unchanged). Dropped frame-0032 (kept frame-0033, overlap 1.0). pytest 23.
        - NOTE: do NOT deploy between 5A/5B (live app.js reads old fields → bare titles); deploy only after 5B.
    - [x] 5B Rendering — DONE (rendering only: app.js + styles.css + static font/transcript assets; no data/schema/structure/grounding/vision change, no deploy).
        - Bullet reader: title + 1-line gist (scan) → bullets as the primary content, each a click-to-verify `<button>` chip (▷ m:ss, keyboard/SR-accessible); blocks nest under their bullet via a left rail tying point + evidence into one unit (long code/tables collapse, diagrams render as the inline frame image — those frames re-extracted at 1920×1080 so labels read). Three-tier verify (always seek-then-play; only the source panel differs): visual → frame + that moment's transcript; spoken → transcript only (no frame — honest); ungrounded → no chip, muted, inert.
        - Editorial LIGHT theme: warm paper + warm ink (never #fff/#000), document-flow sections (whitespace + hairlines, not cards), the evidence block as the one recessed elevation layer; ONE ink-blue accent strictly on verify chips/links/focus; confidence muted (neutral/amber/clay — never "safe"). Self-hosted type spine — Newsreader serif / Hanken Grotesk / JetBrains Mono (OFL woff2 under `static/fonts/`, no CDN). Subtle motion only (animated `<details>` collapse, source-panel cross-fade, staggered page-load), all under `prefers-reduced-motion`. XSS-safe markdown→DOM (headings/bold/code/lists/tables; escape-first, no innerHTML on content).
        - Source-panel consistency: every verify shows "what was said" via `transcriptSentenceAt(ts)` — whole-sentence boundaries (1–2 sentences, trailing "…" only if cut) over a slim served transcript (`static/demo/GDm_uH6VxPY/transcript.json`, built by `scripts/make_static_transcript.py`); used for BOTH visual and spoken. Human-readable caption ("frame-0027 · 6:08 · code extracted by vision"); verify seeks the verify-lead (6:08, not 6:11). Honest `demo` badge (was always "fixture").
        - Table of contents (desktop only, ≥1100px; built dynamically in app.js — index.html untouched): a header hamburger toggles a left drawer of the 5 section titles that reflow-pushes the body to a fixed ~40px past the ToC (`margin-left` anchor — no centered-gutter overshoot, smooth transition); rAF scroll-spy highlights the current section (grayscale, NOT accent; a click locks to the clicked item until the user scrolls; bottom-of-page guard for short trailing sections). Fully removed below 1100px (no toggle/panel/push). Sticky-player column offsets by a shared `--header-h` so it never tucks under the header.
        - Verified locally (headless Chrome + puppeteer-core driving real clicks): all three tiers verify; markdown/diagram/collapse render; fonts load; whole-sentence quotes; ToC reflow-push / scroll-spy / narrow-removal. `pytest -q` green (23; no Python touched).
- [x] 6  4A-prime — change-aware frame sampler (replaced the fixed-interval extraction) — DONE at build time (artifacts regenerated + reader caption); UNCOMMITTED, pending browser sign-off + commit/deploy in Step 7.
    - WHY (unchanged): the old fixed ~14s sampler caught slides mid-reveal (Single-agent Pros/Cons → vision got 1 of 3 cons). Frame selection is the core differentiator, so this is input quality, not polish.
    - MECHANISM — DIVERGED from the original SCOPE after a data-driven design review (the "pHash-cluster → highest-text-score, tie-later" plan was tested on the real frames and corrected):
        - Whole-frame pHash is unusable here — a presenter-video panel on every slide never stops moving (held slides read Δ4–10). So SEGMENTATION runs on a slide-region CROP only (`SLIDE_REGION_FRAC=0.75`); the crop conditions the distance signal ONLY — committed frame, OCR, vision, verify all use the full frame.
        - `SEGMENT_PHASH_THRESHOLD=12` is data-derived (the histogram has NO clean gap; reveal steps ≈12, slide cuts ≥16). 12 keeps a line-by-line reveal inside ONE segment so within-segment selection picks the fully-revealed frame — this, not the threshold, is the primary Pros/Cons fix. Representative per segment = OCR-SUPERSET (`textsim.more_complete_text`, tie→later), with a last-frame fallback for low-text/diagram segments.
        - `app/frame_select.py` gained `crop_to_slide_region` / `segmentation_phash` / `segment` / `choose_representative` / `collapse_adjacent_reps`; the old `DEDUP_HAMMING_THRESHOLD` running-rep dedup is retired; `MIN_TEXT_SCORE=8` kept. Shared text helpers factored into `app/textsim.py`.
    - RE-RUN: `create_demo_artifacts.py` now dense-samples 0.5 fps → segments → commits the representatives; `analyze_demo_frames.py` is the gate-only 4A (new schema: per-rep segment provenance, no `duplicateOf`). Result: 250 dense → **93 representatives → 36 vision candidates** (was 36 frames → 11).
    - VISION-REUSE guard (4B): a new candidate near-identical to a prior committed block (full-frame pHash ≤2 AND |Δt| ≤8s, matched against the OLD 640×360 `frame_analysis` pHash — NOT the on-disk 1080p diagram) REUSES that extraction, remapped to the new frame (vision content reused, every frame field repointed, `reusedFromFrameRef` recorded). Run: **6 reused / 30 fresh ≈ $0.21** + 1 structure call. Resume-cache now validated by `ocrText` (a re-sample reuses frame ids for different content — caught + fixed an id-collision bug). Diagram reps re-sharpened to 1080p after 4B.
    - CORE FIX verified: Single-agent Pros/Cons (`frame-0035`) extracts ALL 3 cons. Heroes survive, picked naturally (not hardcoded): parallel code with the `ParalletAgent→ParallelAgent` OCR fix (`frame-0069`, reused), parallel-architecture diagram @1080p (`frame-0066`), Pros/Cons table (`frame-0082`, reused).
    - CLEAN-CONTENT DEDUP (follow-on; `assemble_note` ONLY — `section_plan` stays the faithful pre-dedup record): same-section, same-type folds to the line-fuller block, re-anchoring the bullet (a fold that would orphan a bullet re-anchors it to the fuller block — never silently ungrounded). Deterministic signals, no LLM/embeddings: text+diagram **token-Jaccard ≥0.65** (`SECTION_DEDUP_JACCARD`; order-independent + immune to Claude's framing wording, which pollutes char-difflib); text PREFIX/subset (zero-loss typewriter fold); code **symmetric difflib ≥0.75** (`SECTION_DEDUP_CODE_RATIO`, `min` of both directions — fixes difflib's ±0.08 order-asymmetry). Folded: root_agent recaptures→`frame-0020`, sushi partial→`frame-0028`, Jin Sho text recaptures→`frame-0053`, contained parallel-code view→`frame-0069`. KEPT (proven distinct): the 5 parallel agent definitions; the two trace diagrams (`0046`~`0047` Jaccard 0.596 < 0.65 — collapsed vs expanded tree).
    - CAPTION (reader, display-only): the source-panel frame caption shows the frame's EXACT `timestampSec`; the verify SEEK still uses the verify-lead (unchanged). No other timestamp touched.
    - STATE: final note **24 placed blocks**, **5 sections**, 18 visual / 4 spoken / 0 ungrounded, 0 stale refs, verify-lead holds; `pytest -q` green (**45**). NOTE: do NOT deploy until committed (live app.js + regenerated data must ship together).
- [ ] 7  Delivery
    arch clean + "deterministic frame selection" rewrite (content-change-driven, not fixed-interval) + deployed smoke run + record numbers + flip status banner + all md + record demo + video.md + submit
    - [x] 7A Architecture clean + docs reconciliation — removed the retired fixed-interval/running-rep `frame_select` path (`DEDUP_HAMMING_THRESHOLD` / `FrameSignals` / `FrameAnalysis` / `frame_signals` / `_gate` / `select` / `analyze_manifest`) + its 4 stale tests; deleted superseded 3B scaffolding (`scripts/build_one_block.py`, `data/demo/GDm_uH6VxPY/{note_3b.json, vision_frame_0027.json}`) + stray `.Rhistory`; readability pass (de-stale `~11` counts, drop the `_line_overlap` alias, group `build_visual_blocks` constants); reconciled APPROACH.md/README.md to the shipped change-aware selector (dense → cropped-pHash segments → representatives → gate) + verified counts. `note_full.json` byte-identical; `pytest -q` 41 green.